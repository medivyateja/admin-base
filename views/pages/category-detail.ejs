<!-- Category Detail content -->
<div class="bg-white shadow-sm rounded-lg p-6">
    <div class="mb-6 flex items-center justify-between">
        <div class="flex items-center space-x-4">
            <div class="flex size-12 items-center justify-center rounded-lg bg-gray-800 text-xl font-medium text-white">
                <%= category.letter %>
            </div>
            <h2 class="text-lg font-semibold text-gray-900"><%= category.title %></h2>
        </div>
        <a href="/categories" class="text-sm text-gray-500 hover:text-gray-700">
            Back to Categories
        </a>
    </div>
    <p class="text-gray-600">Content for <%= category.title %> will go here.</p>
</div>

<%- contentFor('styles') %>
<!-- Add any page-specific styles here -->

<%- contentFor('scripts') %>
<!-- Add any page-specific scripts here -->